# 02-react-cafe

Cafe feedback widget built with React, TypeScript and Vite. Users vote Good / Neutral / Bad and see live stats (per-category counts, total, positive rate).

## Scripts

```bash
npm install
npm run dev      # start dev server
npm run build    # type-check and build for production
npm run lint     # run oxlint
```
